## Day 21

## 21/07/2026

1hr 30min Session
Time - 9:30 to 11:00 am (in college library)

came to college and sat in library directly and started leetcode first thing in the morning with fresh mind
i kinda want to do more leetcode because i like the feeling of coding by hand and trying out logic's and the feeling i get when i solve a question without any hints is pretty great

so told chatgpt i would do the remaining todo questions from yesterday , later today at home and 
we will do a fresh 1hr session now


## Problem 1 - 

### 21. Merge Two Sorted Lists
Easy

took out pen and paper and visualised a lot and came up with this solution completely on my own 
(i feel like linked lists questions are pretty east but they take time to solve)

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode head1 = list1;
        ListNode head2 = list2;
        ListNode resultLL = new ListNode();
        ListNode resultLLHead = resultLL;
        while (head1!=null && head2!=null) {
            if (head1.val <= head2.val) {
                resultLLHead.next = new ListNode(head1.val);
                head1 = head1.next;
                resultLLHead = resultLLHead.next;
            }
            else {
                resultLLHead.next = new ListNode(head2.val);
                head2 = head2.next;
                resultLLHead = resultLLHead.next;
            }
        }
        if (head1 != null) {
            resultLLHead.next = head1;
            resultLLHead = resultLLHead.next;
        }
        if (head2 != null) {
            resultLLHead.next = head2;
            resultLLHead = resultLLHead.next;
        }
        return resultLL.next;
    }
}
```

Runtime
0ms
Beats100.00%
Memory
44.47MB
Beats20.99%

T.C - O(m+n)  // we traverse both the lists , where m and n are lenghts of 2 lists
S.C - O(m+n)  // one node for every copied node , where m and n are lenghts of 2 lists

---

## Problem 2 - 

### 83. Remove Duplicates from Sorted List
Easy

same , took out pen and paper and visualised - 

first thought of using 2 pointers (left,right) and comparing nodes like 2 pointer 
but then realised i dont even need 2 pointers , one is enough
and came up with this - 

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode deleteDuplicates(ListNode head) {
        if (head == null) {
            return head;
        }
        ListNode pointer = head;
        while (pointer.next!=null) {
            if (pointer.val == pointer.next.val) {
                pointer.next = pointer.next.next;
                continue;
            }
            pointer = pointer.next;
        }
        return head;
    }
}
```

Runtime
0ms
Beats100.00%
Memory
45.58MB
Beats32.81%

T.C - O(n)
S.C - O(1)

also from the last few days , what i have observed is -
i am rarely asking hints
never lookig at solution
not watching youtube explanation video

lot of people do these and solve more questions quickly but i am actually learning deeply and solving without cheating or any hints

that's what matters , solving the question in the interview , number of questions solved in leetcode doesnt matter

so i should stop comparing and thinking about others who copy and just continue doing it my way

---

## Problem 3

### 203. Remove Linked List Elements
Easy

still had time and interest so started 3 questions (i shouldnt limit myself to 2 questions per day because some day's i am only doing 1 question so this balances out kinda)

came up with this without any hints - 
```java
class Solution {
    public ListNode removeElements(ListNode head, int val) {
        ListNode pointer = head;
        while (pointer != null) {
            if (pointer.next.val == val) {
                pointer.next = pointer.next.next;
            }
            pointer = pointer.next;
        }
        return head;
    }
}
```

but was having issues with [7,7,7,7] case , the first 7 node is always being left out

tried doing using pointer.val == val and using previous node pointer but i wasnt actually manipulating the original array here  

```java
class Solution {
    public ListNode removeElements(ListNode head, int val) {
        ListNode pointer = head;
        ListNode previous = head;
        while (pointer != null) {
            if (pointer.val == val) {
                previous.next = pointer.next;
            }
            previous = pointer.next;
            pointer = pointer.next;
        }
        return head;
    }
}
```

scrolled through leetcode comments (not hints or solution) 
and someone said - 

just add dummy node at front and solve with loop as usual

didnt understand on the first glance and kept on looking in comments until i found this - 

Approach: Dummy Node + Iterative Traversal

Removing nodes becomes tricky when the node to be removed is the head.

Introduce a dummy node before the head:
dummy -> head


Algorithm Steps:

Create: dummy -> head

Initialize: current = dummy

If:
    current.next.val == val
    remove the node:

Return: dummy.next


Exactly what i was doing , but by adding dummy node , we can also compare the head node 
understood it now and solved - 

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode removeElements(ListNode head, int val) {
        ListNode dummy = new ListNode(0,head);
        ListNode pointer = dummy;
        while (pointer!=null && pointer.next != null) {
            if (pointer.next.val == val) {
                pointer.next = pointer.next.next;
            }
            else {
            pointer = pointer.next;
        }
        }
        return dummy.next;
    }
}
```

Runtime
1ms
Beats96.90%
Memory
46.87MB
Beats97.83%

T.C - O(n)
S.C - O(1)

tbh , i never came pattern
using a dummy node 

first time , but now as i came across it , ill keep in mind 
didnt know dummy nodes will have its own pattern
