## Day 4

## 04/07/2026

30min Session

i completely forgot to do leetcode today until after i slept on bed
then suddenly remembered and i didnt wanna break my streak , cuz i didnt wanna make a "choice" of skipping a day

so woke up from bed and opened pc , its very late (11:00 pm) so told chatgpt lets do one quick question for today
it suggested this ->

## Problem 1 -

### 349. Intersection of Two Arrays

Easy

i actually never attempted this problem before so completely new

chatgpt gave this hint - Topic: HashSet
leetcode topics - Array , Hash Table , Two Pointers , Binary Search , Sorting

so tried multiple solutions

Attempt 1 (stopped midway)
```java
class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        HashSet<Integer> set1 = new HashSet<>();
        HashSet<Integer> set2 = new HashSet<>();
        List<Integer> result = new ArrayList<>();
        for (int i =0;i<nums1.length;i++) {
            set1.add(nums1[i]);
        }
        for (int j =0;i<nums2.length;j++) {
            set2.add(nums2[j]);
        }

        HashMap<Integer,Integer> map = new HashMap();
    }
}
```
Attempt 2 
```java
class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        List<Integer> result = new ArrayList<>();

        HashMap<Integer,Integer> map = new HashMap();
        HashSet<Integer> set = new HashSet<>();
 
        for (int i =0;i<nums1.length;i++) {
            map.put(nums1[i],1);
        }
        for (int j =0;j<nums2.length;j++) {
            if (map.containsKey(nums2[j])) {
                result.add(nums2[j]);
            }
        }

        for (int k =0;k<result.size();k++) {
            set.add(result[k]);
        }
        for (int l =0;l<set.size();l++) {
            set.add(result[k]);
        }
        return arr;

    }
}
```
Attempt 3 (first submission , some test cases failed tho)
```java
class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        HashSet<Integer> result = new HashSet<>();
        HashSet<Integer> set = new HashSet<>();
 
        for (int i =0;i<nums1.length;i++) {
            set.add(nums1[i]);
        }

        for (int j =0;j<nums2.length;j++) {
            if (!set.add(nums2[j])) {
                result.add(nums2[j]);
            }
        }
        int[] resultArr = new int[result.size()];
        int index = 0;
        for (int k : result) {
            resultArr[index] = k;
            index++;
        }
        return resultArr;
    }
}

```

Attempt 4 (working but very inefficient)

```java
class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        HashSet<Integer> result = new HashSet<>();
        HashSet<Integer> set1 = new HashSet<>();
        HashSet<Integer> set2 = new HashSet<>();
 
        for (int i =0;i<nums1.length;i++) {
            set1.add(nums1[i]);
        }
        for (int l =0;l<nums2.length;l++) {
            set2.add(nums2[l]);
        }

        for (int num : set2) {
            if (!set1.add(num)) {
                result.add(num);
            }
        }
        int[] resultArr = new int[result.size()];
        int index = 0;
        for (int k : result) {
            resultArr[index] = k;
            index++;
        }
        return resultArr;
    }
}
```

Runtime
3ms
Beats47.72%
Memory
45.63MB
Beats5.79%


too late to deeply analyse and try out different solutions (its 11:40pm now)
so i'll continue working on this problem tomorrow