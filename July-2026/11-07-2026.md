

class Solution {
    public int maxArea(int[] height) {
        int maxValue = 0;
        int maxPointerIndex = 0;
        for (int i =0;i<height.length;i++) {
            if (height[i] > maxValue) {
                maxValue = height[i];
                maxPointerIndex = i;
            }
        }
        int maxArea =0;
        int maxAreaPointerIndex = 0;

        for (int j =0;j<height.length;j++) {
            int area = Math.min(height[j] , maxValue) * Math.abs(maxPointerIndex - j);
            if (area > maxArea) {
                maxArea = area;
                maxAreaPointerIndex = j;
            }
        }
        return maxArea;
        
    }
}

came up with this but

error with this test case -> [1,2,1]

we are calculating highest height and then calculation area so using 1,2 area is 1 , but if we use 1,1 then area is 2 



also tried brute forcing 

class Solution {
    public int maxArea(int[] height) {
        int maxArea = 0;
        for (int i=0;i<height.length;i++) {
            for (int j=0;j<height.length;j++) {
                int area = Math.min(height[i],height[j]) * Math.abs(i-j);
                if (area > maxArea) {
                    maxArea = area;
                }
            }
        }
        return maxArea;
    }
}

but time limit exceeded for some huge testcase (some 1000 numbers in an array) like CRAZY BIG input

watched this video - https://youtu.be/UuiTKBwPgAo?si=rPrY1Yf2aAhuKMd5 

hmm , tbh didn't fully understand how he got the idea of using two pointers tho , like looking at it from isolation , idk , maybe just solving similar problem before and resolving it gets you the idea that i solved this like this last time etc? 

after watching explanation from video , came up with t his - 

class Solution {
    public int maxArea(int[] height) {
        int left = 0;
        int right = height.length-1;
        int maxArea = 0;
        while (left < right) {
            int area = Math.min(height[left],height[right]) * Math.abs(left-right);
            if (area > maxArea) {
                maxArea = area;
            }
            
            if (height[left] <= height[right]) {
                left++;
            }
            else if (height[left] > height[right]) {
                right--;
            }
        }
        return maxArea;
    }
}

Runtime
6ms
Beats5.04%
Memory
77.14MB
Beats85.26%

but this is the most common runtime tho (77% solutions used 5ms runtime)



problem 2 

977. Squares of a Sorted Array


i had to use two pointer approach

how do you even use two pointer approach here 

so found this in comments of Leetcode and then clicked ohhh - https://robertocannella-f5b4b.web.app/coding-challenges/sorted-squares

like i already did this before but using sorting and squaring 



class Solution {
    public int[] sortedSquares(int[] nums) {
        int[] arr = new int[nums.length];
        int left = 0;
        int right = nums.length-1;
        int index = nums.length-1;
        while (index >= 0) {
            int leftSquare = nums[left]*nums[left];
            int rightSquare = nums[right]*nums[right];
            if ( leftSquare >= rightSquare ) {
                arr[index] = leftSquare;
                index--;
                left++;
            }
            else if ( leftSquare < rightSquare ) {
                arr[index] = rightSquare;
                index--;
                right--;
            }
        }
        return arr;
    }
}

Runtime
1ms
Beats100.00%
Memory
46.96MB
Beats94.56%


BUT MAN , how am i supposed to come up with this man
like OMG , one of the most depressing day
this makes no sense at all 

