## Day 20

## 20/07/2026

1hr Session
Time - 5:30 to 6:30 pm

new topic from today 
Linked Lists

Node Syntax - 

```java
class ListNode {
    int val;
    ListNode next;
}
```

Node Syntax used in most questions - 

```java
public class ListNode {
    int val;
    ListNode next;
    ListNode() {}
    ListNode(int val) { this.val = val; }
    ListNode(int val, ListNode next) { this.val = val; this.next = next; }
}
```

Traversal Pattern - 

```java
ListNode current = head;

while (current != null) {
    // do something

    current = current.next;
}
```

---

## Problem 1.

### 206. Reverse Linked List
Easy

felt easy enough on first glance 
i had a lot of practice with linked lists when i started learning dsa in 2nd year but didnt touch / revise for a long time

so started without any hints 

took out pen and paper and visualised for so long , first approach , tried in place reversing 
tried that for way too long like rough idea -> 

temp = current.next;
current.next = null;
head = temp.next;
temp.next = current;
head.next = temp;
current = head;

was very messy + lot of issues with it 

then realised , i should just take another linked list and keep adding nodes to it (not the most optimal but good starting point)
so even for that , did a lot of dry runs of code and visualization using pen and paper 

and finally came up with this - 

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode reverseList(ListNode head) {
        if (head == null) {
            return null;
        }
        ListNode secondLL = new ListNode(head.val);
        ListNode temp = new ListNode();
        head = head.next;
        while (head != null) {
            temp = secondLL;
            secondLL = new ListNode(head.val);
            secondLL.next = temp;
            head = head.next;  
        }
        return secondLL;
    }
}
```

Runtime
0ms
Beats100.00%
Memory
44.21MB
Beats50.39%

T.C - O(n)
S.C - O(n)

MANNN , need a lot of practice , the above thing took me 45 minutes , my grip on linkedlists is gone

TODO - 
in place reverse of linkedlist 
and also question mentioned - Follow up: A linked list can be reversed either iteratively or recursively. Could you implement both?


---

## Problem 2

### 876. Middle of the Linked List
Easy

while giving description of this problem , chatgpt literally gave away the hint!!

It introduces one of the most important Linked List techniques:
Fast & Slow Pointers.

You'll use this pattern again in:

Cycle Detection
Merge Sort on Linked Lists
Palindrome Linked List
Reorder List
Find Cycle Start
Many interview questions

also told this in the next - 
I have a feeling you'll first come up with the two-pass solution (count length, then traverse again), and then you'll discover there's a much nicer one-pass idea.

LITERALLY GAVE AWAY BOTH APPROACHES MAN , told it to not give hints like that and just tell me the question number and thats it 

anyway , started with the two pass solution - 

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode middleNode(ListNode head) {
        ListNode pointer = head;
        int count = 0;
        while (pointer != null) {
            count++;
            pointer = pointer.next;
        }
        count = (count/2);
        pointer = head;
        while (count > 0) {
            pointer = pointer.next;
            count--;
        }
        return pointer;
    }
}
```

Runtime
0ms
Beats100.00%
Memory
42.92MB
Beats35.62%

T.C - O(n)
S.C - O(1) (cuz we are only using 1 pointer , i thought the pointer stores the entire linkedlist O(n) , but it only stores reference to original one)

ill do the fast and slow pointer approach when i learn fast and slow approach (in this week)