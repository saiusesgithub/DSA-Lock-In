## Day 23

## 23/07/2026

1hr Session
Time - 10:15 to 10:45pm

today in college we had ecell website launch event so couldn't sit in library and do leetcode

after coming home , was soo tierd , almost slept , couldnt even sit right let alone focus and do leetcode
so entire day timepass , but still sat to do leetcode at night 
didnt wanna break the streak

## Problem 1 - 

### 19. Remove Nth Node From End of List

Medium

came up with this (took out book and pen and dry ran code to find out small logic errors etc) 

```java
class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
        if (head == null) {
            return null;
        }
        ListNode dummyHead = new ListNode(0,head);
        ListNode pointer = dummyHead;
        int count = 0;
        while (pointer != null) {
            count++;
            pointer = pointer.next;
        }
        int removeCount = count - n - 1;
        pointer = dummyHead;
        while (removeCount > 0) {
            pointer = pointer.next;
            removeCount--;
        }
        pointer.next = pointer.next.next;
        return dummyHead.next;
         
    }
}
```

Runtime
0ms
Beats100.00%
Memory
43.74MB
Beats6.76%

T.C - O(n)
S.C - O(1)

description said -

Follow up: Could you do this in one pass?

its too late now , ill do the other approaches for this problem along with other pending todo's