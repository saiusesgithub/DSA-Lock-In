## Day 35

## 04/08/2026

45min Session
Time - 10:30 to 11:15pm

went to the dentist in the morning and then went to startups india office (internship) and then went out to eat dinner and then startups india meet and by the time all this was completed , time was 10:30

no time to study trees so started doing leetcode problem to maintain streak 
and man i am really proud of this habit , like it literally became a habit , i dont overthink about it anymore , automatically do it everyday no matter how tired i am

i gotta convert german learning and aws learning into that kind of habit 

Today's DSA problem:

## Problem 1 - 

### 1790. Check if One String Swap Can Make Strings Equal
Easy

didnt look at any hints , kinda thought of array implementation first but then this approach clicked - 
took out pen and paper and dry ran code and came up with this -

```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        int notEqualCount = 0;
        char s1NotEqual = 'a';
        char s2NotEqual = 'b';
        for (int i =0;i<s1.length();i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                notEqualCount++;
                s1NotEqual = s1.charAt(i);
                s2NotEqual = s2.charAt(i);

            }
            if (notEqualCount > 1) {
                if (s2.charAt(i) != s1NotEqual || s1.charAt(i) != s2NotEqual) {
                    return false;
                }
            }
        }
        return true;
    }
}
```

error with this test case - 
```
bank
kanb
```

so came up with this - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        int notEqualCount = 0;
        char s1NotEqual = 'a';
        char s2NotEqual = 'b';
        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                notEqualCount++;
                if (notEqualCount > 1) {
                    if (s2.charAt(i) != s1NotEqual || s1.charAt(i) != s2NotEqual) {
                        return false;
                    }
                }
                s1NotEqual = s1.charAt(i);
                s2NotEqual = s2.charAt(i);

            }

        }
        return true;
    }
}
```

Error - 

Input
s1 =
"appla"
s2 =
"lppal"

Output
true
Expected
false


visualised in python tutor and came up with this - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        int notEqualCount = 0;
        char s1NotEqual = 'a';
        char s2NotEqual = 'b';
        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                notEqualCount++;
                if (notEqualCount > 1) {
                    if (s2.charAt(i) != s1NotEqual || s1.charAt(i) != s2NotEqual) {
                        return false;
                    }
                    notEqualCount = 0;
                    s1NotEqual = 'a';
                    s2NotEqual = 'a';
                }
                s1NotEqual = s1.charAt(i);
                s2NotEqual = s2.charAt(i);

            }

        }
        return (notEqualCount==0);
        // return true;
    }
}
```

Wrong Answer
136 / 137 testcases passed
Input
s1 =
"qgqeg"
s2 =
"gqgeq"

Output
true
Expected
false

then came up with this - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        int notEqualCount = 0;
        char s1NotEqual = 'a';
        char s2NotEqual = 'b';
        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                if (notEqualCount == -1) {
                    return false;
                }
                notEqualCount++;
                if (notEqualCount > 1) {
                    if (s2.charAt(i) != s1NotEqual || s1.charAt(i) != s2NotEqual) {
                        return false;
                    }
                    notEqualCount = -1;
                    s1NotEqual = 'a';
                    s2NotEqual = 'a';
                }
                s1NotEqual = s1.charAt(i);
                s2NotEqual = s2.charAt(i);

            }

        }
        return true;
        // return true;
    }
}
```

Error - 
Input
s1 =
"aa"
s2 =
"ac"

Output
true
Expected
false

FINALLY
came up with this, completely on my own , NO hints :) - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        int notEqualCount = 0;
        char s1NotEqual = 'a';
        char s2NotEqual = 'b';
        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                if (notEqualCount == -1) {
                    return false;
                }
                notEqualCount++;
                if (notEqualCount > 1) {
                    if (s2.charAt(i) != s1NotEqual || s1.charAt(i) != s2NotEqual) {
                        return false;
                    }
                    notEqualCount = -1;
                    s1NotEqual = 'a';
                    s2NotEqual = 'a';
                }
                s1NotEqual = s1.charAt(i);
                s2NotEqual = s2.charAt(i);

            }

        }
        return (notEqualCount == 0 || notEqualCount == -1);
        // return true;
    }
}
```

Runtime
0ms
Beats100.00%
Memory
43.10MB
Beats43.12%

T.C - O(n)
S.C - O(1)




after the problem was solved , looked at optimal solutions in leetcode - 

most optimal solution - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
        if (s1.equals(s2)) return true;

        int first = -1, second = -1;

        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                if (first == -1) {
                    first = i;
                } else if (second == -1) {
                    second = i;
                } else {
                    return false; // More than 2 mismatches
                }
            }
        }

        return second != -1 &&
               s1.charAt(first) == s2.charAt(second) &&
               s1.charAt(second) == s2.charAt(first);
    }
}
```

other approach (not as good as my solution) - 
```java
class Solution {
    public boolean areAlmostEqual(String s1, String s2) {
       if (s1.length() != s2.length()) {
            return false;
        }

        if (s1.equals(s2)) {
            return true;
        }

        ArrayList<Integer> n = new ArrayList<>();

        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i) != s2.charAt(i)) {
                n.add(i);
            }
        }

        if (n.size() != 2) {
            return false;
        }

        char[] t = s2.toCharArray();

        int left = n.get(0);
        int right = n.get(1);

        char temp = t[left];
        t[left] = t[right];
        t[right] = temp;

        return s1.equals(new String(t));
    }
}
```

after reading above journal , chatgpt said - 

Good debugging session. You kept narrowing the issue until all three valid outcomes were handled:

* zero mismatches → strings already equal
* exactly two mismatches that cross-match → one swap works
* anything else → false

Your accepted state encoding works:

```text
0  = no unmatched difference yet
1  = saw one difference
-1 = successfully matched exactly one swap
```

But it is harder to read than necessary because one variable is doing two jobs: counting mismatches and representing “swap completed.”

The key invariant is simpler:

> There must be either exactly 0 mismatched positions or exactly 2 mismatched positions.

For the two mismatches at indices `a` and `b`:

```text
s1[a] == s2[b]
s1[b] == s2[a]
```



